// Used by the Next.js build (Vercel). The Vite/vinext build configures the same
// plugin inline in vite.config.ts, so it ignores this file.
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
