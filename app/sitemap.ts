import type { MetadataRoute } from 'next';
import { previewCollections, previewProducts } from '../lib/commerce';
export default function sitemap(): MetadataRoute.Sitemap { const base = 'https://www.bioloark.co.il'; return [{ url: base, changeFrequency: 'weekly', priority: 1 }, { url: `${base}/project-showcase`, changeFrequency: 'monthly', priority: .7 }, { url: `${base}/policies/refund-policy`, changeFrequency: 'yearly', priority: .4 }, { url: `${base}/policies/terms-of-service`, changeFrequency: 'yearly', priority: .4 }, { url: `${base}/policies/accessibility-statement`, changeFrequency: 'yearly', priority: .4 }, ...previewCollections().map((collection) => ({ url: `${base}/category/${encodeURIComponent(collection.handle)}`, changeFrequency: 'weekly' as const, priority: .8 })), ...previewProducts().map((product) => ({ url: `${base}/product-page/${encodeURIComponent(product.handle)}`, changeFrequency: 'weekly' as const, priority: .7 }))]; }
